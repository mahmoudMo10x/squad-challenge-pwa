# تشغيل ونشر MVP الأونلاين

## التطوير المحلي

شغّل الخادم والواجهة في نافذتين:

```bash
npm run dev:server
npm run dev
```

الواجهة على `http://localhost:5173` والخادم على `http://localhost:3001`. افتح نافذتين أو جهازين واضغط «لعبة جديدة أونلاين» في كليهما.

## إنتاج حزمة واحدة

```bash
npm run build:all
npm start
```

بعدها يخدم Node الواجهة وSocket.IO من المنفذ نفسه. نقطة الصحة: `/health`.

## متغيرات البيئة

راجع `.env.example`. في الإنتاج اضبط `ALLOWED_ORIGINS` على نطاق اللعبة فقط. لا تضع `VITE_SERVER_URL` عند تقديم الواجهة والخادم من النطاق نفسه.

## Docker

```bash
docker build -t squad-challenge .
docker run --rm -p 3001:3001 --env-file .env squad-challenge
```

## حدود هذه النسخة

- الغرف في ذاكرة عملية واحدة؛ إعادة تشغيل الخادم تنهي المباريات.
- لا تدعم عدة نسخ خادم أو Redis بعد.
- الهوية Session مجهولة وليست نظام حسابات موثقًا.
- قاعدة 500 لاعب خيالية ومولدة منهجيًا، لكنها تحتاج مراجعة بشرية للتقييمات والأسماء قبل الإطلاق العام.
- لم تُجرَ بعد اختبارات بصرية يدوية على أجهزة iOS وAndroid فعلية.

هذه القيود مقبولة لاختبار Alpha محدود، لكنها تمنع وصف النسخة بأنها جاهزة لإطلاق عام واسع.
